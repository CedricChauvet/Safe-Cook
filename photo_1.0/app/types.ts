/// <reference types="expo-router" />

// Define a type or interface
export interface MyComponentProps {
    prop1: string;
    prop2: number;
  }

  // Export the type or interface as default
  export default MyComponentProps;